name: myfirstgithubaction
on:
  push
 
jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: checkout 
        uses: actions/checkout@v6
      - name: welcomejob
        run: echo "My first job"
      - name: listfiles
        run: ls -lart
      - name: readfile
        run: cat README.md