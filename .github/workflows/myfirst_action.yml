name: myfirstgithubaction
on:
  push:
  workflow_dispatch:
jobs:
  first_job:
    runs-on: ubuntu:latest
    steps:
      - name: welcomejob
        run: echo "My first job"
      - name: listfiles
        run: ls -lart
      - name: readfile
        run: cat README.md